materials = {
    'line': '''
<b>📌 Линейные уравнения: ключевые фишки</b>

<i>Формат:</i> ax + b = 0 → x = -b/a  

🚀 <u>Главные правила:</u>
1. <b>Знаки — только в конце!</b> Решай уравнение без учёта минусов, как будто все коэффициенты положительные.  
   Пример: <code>-5x - 5 = -45</code> → 5x = 40 → x = 8 (знак уже учтён в коэффициентах).

2. <b>Дроби — не проблема:</b> Если ответ должен быть целым, коэффициенты «подскажут» решение:  
   <code>-2.6x = -20.8</code> → x ≈ 8 (2.6 × 8 = 20.8).

3. <b>Устный счёт:</b> Используй таблицу умножения и округления:  
   • <code>7x = 105</code> → x=15 (7×10=70 → 105-70=35 → 35/7=5 → 10+5=15).  
   • Для чисел вида 2.6x: округли до 2.5x для быстрого прикида.

4. <b>Проверка:</b> Подставь ответ в исходное уравнение — займёт 5 секунд, но спасёт от ошибки.
    ''',

    'quadratic': '''
<b>📌 Квадратные уравнения: рациональное решение</b>

🚀 <u>Эффективные методы:</u>
1. <b>Разложение свободного члена:</b>
   Для уравнения <code>x² + bx + c = 0</code>:
   - Найдите два числа <i>m</i> и <i>n</i>, где <i>m·n = |c|</i> и <i>m + n = |b|</i>.
   - Учтите знаки: если <code>c &gt; 0</code> → оба числа одного знака, если <code>c &lt; 0</code> → разных.
   
   Пример: <code>x² + 12x - 64 = 0</code>  
   <code>64 = 16×4</code>, <code>16 - 4 = 12</code> → корни: <b>-16</b> и <b>4</b>.

2. <b>Сокращение коэффициентов:</b>
   Если все коэффициенты делятся на <i>k</i>, разделите уравнение на <i>k</i>:
   <code>3x² - 21x + 30 = 0 → x² - 7x + 10 = 0</code>.

3. <b>Выбор корня:</b> В ответе укажите наименьший корень.
    ''',

    'proportion': '''
<b>📌 Пропорции: строгое решение</b>

🚀 <u>Алгоритм работы:</u>
1. <b>Основное свойство пропорции:</b>
   <code>a/b = c/d → a·d = b·c</code>

2. <b>Сокращение дробей:</b>
   Перед решением упростите дроби:
   <code>16/32 = x/8 → 1/2 = x/8 → x = 4</code>.

3. <b>Десятичные дроби:</b>
   Умножьте все части пропорции на 10ⁿ, чтобы получить целые числа:
   <code>0.3/x = 1.5/5 → 3/x = 15/50 → x = 10</code>.
    ''',

    'powers': '''
<b>📌 Уравнения вида x² = N</b>

🚀 <u>Пошаговый анализ:</u>
1. <b>Определение диапазона:</b>
   - Найдите ближайшие квадраты целых чисел:  
     <code>40² = 1600</code>, <code>50² = 2500</code> → для 2025: 45².

2. <b>Анализ последней цифры:</b>
   - 0 → 0; 1 → 1/9; 4 → 2/8; 5 → 5; 6 → 4/6; 9 → 3/7.

3. <b>Стандартные случаи:</b>
   - <code>x² = 121</code> → между 10² и 15² → 11².  
   - <code>x² = 625</code> → 25² (правило для чисел, оканчивающихся на 25).

<u>Требования ОГЭ:</u>  
• Ответ всегда целый → проверяйте только целочисленные корни.  
• Учитывайте оба решения (±x), если нет ограничений в условии.
    '''
}